***FIFO Adder System***

A simple C++ console-based system that simulates a FIFO-based adder using queues.The program loads elements from two input vectors into FIFO buffers and performs pairwise addition based on user commands.The final results are stored in a third vector and displayed at the end.
